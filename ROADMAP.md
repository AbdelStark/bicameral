# Roadmap

## v0.1 MVP — shipped

System 1 runtime + R1 Gate, R2 Honest Finish, R3 Stuck Detector, HUD/`/why` data, judgment packs, FakeBackend + TypeSafeBackend. Testable without live Jev or a human TUI.

## Next: v0.2

- R4 Toolpack Router (`pi.setActiveTools` additive)
- R5 Model and Effort Router (`before_agent_start`)
- R6 Injection Shield (`tool_result` wrap)
- Standalone `bicameral` CLI on the Pi SDK
- LLM System 1 backend (same packs; H5 comparison)

## Then: v0.3

- R7 Context Curator + `recall`
- R8 Best-of-N / `/fanout`
- SWE-bench / TamperBench / StallBench runners and the pre-registered report (`PREREGISTRATION.md`)
