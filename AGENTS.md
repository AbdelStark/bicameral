# Bicameral

Community project, private until launch.

Read `PRD.md` and `SPEC.md` before writing code. Implementation happens in this repository.

- Pi package `pi-bicameral` and standalone CLI `bicameral`
- Built on Pi (`@earendil-works/pi-coding-agent` 0.85.x) and `@typesafe-ai/sdk` 0.6.x
- Public Pi extension and SDK APIs only; do not fork Pi
- System 1 never generates text

Do not add TypeSafe job-hunt or campaign material to this repo.

